#include "activity/home_activity.hpp"

// Activity is fully defined in the header (CONTENT_FROM_XML_RES does the
// work). This translation unit exists so the build system has a non-empty
// .cpp to compile and so non-trivial Phase B+ activity logic can land here
// without a follow-up CMake change.
